export type PlayerStorageDTO = { 
  // Data Transfer Object -> É um conceito que vem do mundo do Java, e que basicamente é uma classe que serve para transferir dados entre sistemas.
  name: string;
  team: string;
}

